on:
  push:
    branches:
      - main  # Or whichever branch you want

jobs:
  hello:
    name: Say Hello Everyone, WELCOME ALL FROM TELUS DIGITAL!
    runs-on: self-hosted

    steps:
      - name: Print Hello World
        run: echo "Hello, World from self-hosted runner!"
